#footer-wrap
  if theme.footer.owner.enable
    - var now = new Date()
    - var nowYear = now.getFullYear()
    if theme.footer.owner.since && theme.footer.owner.since != nowYear
      .copyright!= `&copy;${theme.footer.owner.since} - ${nowYear} By ${config.author}`
    else
      .copyright!= `&copy;${nowYear} By ${config.author}`
  if theme.footer.copyright
    .framework-info
      span= _p('footer.framework') + ' '
      a(href='https://hexo.io')= 'Hexo'
      span.footer-separator |
      span= _p('footer.theme') + ' '
      a(href='https://github.com/jerryc127/hexo-theme-butterfly')= 'Butterfly'
  if theme.footer.beian
    .beian-info
      a(href='http://beian.miit.gov.cn')= `${theme.footer.beian}`
  if theme.badge.enable
    .badge-line
    each item in theme.badge.badgeitem
      a(target='_blank' href=url_for(item.link)  style='margin-inline:5px')
        img(src=url_for(item.shields) title=item.massage)
  if theme.footer.custom_text
    .footer_custom_text!=`${theme.footer.custom_text}`
